#include <iostream>

using namespace std;

int main() {
    int t;
    for (int i = 0; i < t; i++) {
        // Input
        int n, m;
        cin >> n >> m;
        int* arr = new int[n];
        for (int j = 0; j < n; j++) {
            cin >> arr[j];
        }

        //Calculation
        for (int j = 0; j < n; j++) {
            
        }

    }
    return 0;
}