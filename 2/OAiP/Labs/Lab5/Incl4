// Output matrix on screen
procedure writeMatrix(M: TMatrix);
var
  i,j: Integer;
begin
  for i := 1 to 3 do
  begin
    for j := 1 to 3 do
      write(M[i,j]:3, ' ');
    writeln;
  end;

end;